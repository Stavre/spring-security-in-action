info:
  name: Test user natalie cannot see other users secret names
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8080/secret/names/emma
  auth:
    type: basic
    username: natalie
    password: "123"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
