info:
  name: Test user natalie can see its own secret names
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/secret/names/natalie
  auth:
    type: basic
    username: natalie
    password: "123"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
