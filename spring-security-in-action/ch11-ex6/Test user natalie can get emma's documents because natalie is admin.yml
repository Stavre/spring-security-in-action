info:
  name: Test user natalie can get emma's documents because natalie is admin
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8080/documents/asd555
  auth:
    type: basic
    username: natalie
    password: "123"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
