info:
  name: Test user with read is not authorized
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/hello
  auth:
    type: basic
    username: natalie
    password: "123"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
