info:
  name: Test user with write is authorized
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8080/hello
  auth:
    type: basic
    username: emma
    password: "123"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
