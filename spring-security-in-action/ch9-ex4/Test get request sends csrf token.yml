info:
  name: Test get request sends csrf token
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/hello
  headers:
    - name: X-IDENTIFIER
      value: "12345"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
