info:
  name: get token list
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8080/hellotoken
  headers:
    - name: X-IDENTIFIER
      value: "12345"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
