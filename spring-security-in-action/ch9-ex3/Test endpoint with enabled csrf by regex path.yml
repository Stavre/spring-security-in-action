info:
  name: Test endpoint with enabled csrf by regex path
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/tschuss
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
