info:
  name: Test endpoint with enabled csrf by hardcoded path
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/hello
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
